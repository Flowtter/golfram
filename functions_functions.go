package main

/*
func recognize(expression string) []string {
	// using x->x+2 format
	function := strings.Split(expression, "->")
	/*
	if len(function) != 2 {
		fmt.Print("Expression not compatible (%s)", expression)
	}
	return function
}
*/
